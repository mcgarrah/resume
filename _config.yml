#Update all the sections by editing the data.yml file inside _data folder.

# Site Settings
title: McGarrah Resume
url: 'https://mcgarrah.org'
baseurl: "/resume"

# Canonical URL configuration for SEO
canonical_url: "https://mcgarrah.org/resume"

description: A beautiful Jekyll theme for creating a Resume
# Style will be applied only after restarting the build or serve. Just choose one of the options.
theme_skin: ceramic # blue turquoise green berry orange ceramic burgundy darkred dark (dark needs work)
chrome_mobile_color: #use hex colors (ex:#1976d2) or leave empty if you don't want a color for chrome mobile searchbar

# Google Analytics
google_analytics: G-F90DVB199P

# Sass/SCSS
sass:
  sass_dir: _sass
  style: compressed # http://sass-lang.com/documentation/file.SASS_REFERENCE.html#output_style

# Build settings
compress-site: yes

encoding: "utf-8"
compress_html: # - http://jch.penibelst.de/
  clippings: all
  ignore:
    envs: development

# Development Settings
port: 4000
#host: 0.0.0.0
host: 127.0.0.1
safe: false

# Jekyll Plugins
plugins:
  - jekyll-pandoc-exports

# Pandoc Exports Plugin Configuration
pandoc_exports:
  enabled: true
  pdf_options:
    variable: 'geometry:margin=0.75in'
  unicode_cleanup: true
  inject_downloads: true
  download_class: 'pandoc-downloads no-print'
  title_cleanup:
    - '<title>.*?</title>'
    - '<h1[^>]*>.*?McGarrah Resume.*?</h1>'
  image_path_fixes:
    - pattern: 'src="/resume/assets/images/'
      replacement: 'src="{{site.dest}}/assets/images/'
